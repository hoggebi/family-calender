# 호 가족 일정 (family-calendar) — 프로젝트 인수인계 문서

> 이 파일은 저장소 루트에 `CLAUDE.md`로 두면 Claude Code가 프로젝트를 열 때 자동으로 읽습니다.

## 1. 프로젝트 목적

가족(호 할아버지 / 호 할머니 / 호 엄마)이 함께 쓰는 공유 캘린더 웹앱.

원래는 Claude.ai 대화 안의 아티팩트(HTML + `window.storage`)로 만들었으나, **클로드 안에서만 작동하고 다른 사람 기기에는 데이터가 공유되지 않는 근본적 한계**에 부딪혀, 이미 운영 중이던 "오늘도 호 English"(kids-english-reader) 앱과 **동일한 스택으로 재구현해 Vercel에 실제 배포**했다. 그래야 클로드 없이 아무 브라우저에서나 열어도 실시간으로 저장·공유된다.

- 배포 주소: https://family-calender-gamma.vercel.app
- GitHub 저장소: `hoggebi/family-calender` (오타로 "calendar"가 아니라 "calender"로 만들어졌고, 이미 배포된 상태라 그대로 유지 중)

## 2. 현재 구현된 기능

- **고정 구성원 3명**: 호 할아버지(👨) / 호 할머니(👩) / 호 엄마(🤓). 추가·삭제 UI는 없고 코드(`lib/types.ts`)에 하드코딩되어 있음.
- **월별 캘린더 뷰**: 일요일·공휴일은 빨간색, 토요일은 파란색. 공휴일 이름은 `lib/holidays.ts`에 2025~2027년 데이터가 들어있음(설날·추석 등 음력 명절, 대체공휴일 포함).
- **일정 등록 두 가지 경로**:
  1. 달력의 날짜 칸을 눌러서 그 자리에서 추가/수정/삭제 (제목, 시간, 담당자, 메모, 그리고 "종료일"을 선택하면 그 자리에서도 여러 날짜 일정을 만들 수 있음)
  2. 화면 하단 "일정 빠르게 추가" — 담당자·제목·일정 종류(하루/여러 날/매주 반복)·날짜(또는 요일)를 직접 선택해서 등록. **AI 없이 즉시 저장됨.**
- **여러 날짜에 걸친 일정**은 날짜마다 따로 표시되지 않고, 같은 주 안에서는 시작일부터 끝나는 날까지 **하나로 이어진 막대**로 표시됨 (주가 바뀌면 다음 줄에서 새 막대로 이어짐).
- **매주 반복 일정**: 요일을 여러 개 선택해 등록, 화면 하단 "🔁 반복 일정" 목록에서 개별 삭제 가능.
- **이번달 주요 공지사항**: 텍스트를 적을 때마다 그 달의 공지 목록에 계속 쌓임(덮어쓰지 않음). 날짜를 직접 선택하면 그 날짜의 달력 칸에도 📌 표시로 자동 연동됨. 달을 이동하면 그 달에 쌓인 공지만 보임.
- **데이터 저장**: Upstash Redis에 통째로 저장(`family-calendar:data:v1` 키 하나에 members/events/recurring/notices 전체를 JSON으로 저장·조회). 새로고침해야 최신 상태 반영(실시간 push는 아님).
- **모바일 전용 레이아웃**: 화면 폭 최대 430px로 고정(데스크톱에서 열어도 폰 화면처럼 좁게 보임), 입력창 폰트 16px로 iOS 확대 방지, 터치하기 쉬운 버튼 크기.
- **디자인**: 프리텐다드(Pretendard) 폰트, 전체 화이트/블랙 톤 + 일요일·공휴일(빨강)·토요일(파랑)만 포인트 컬러. 타이틀 "호 가족 일정"은 프리텐다드 600 굵기.

## 3. 수정한 주요 파일

| 파일 | 역할 |
|---|---|
| `app/page.tsx` | 전체 UI와 상태 관리(가장 크고 핵심적인 파일). 캘린더 그리드, 공지사항, 빠른입력, 일정 모달이 전부 여기 있음 |
| `app/calendar.css` | 위 페이지 전용 스타일 (레이아웃, 색상, 반응형) |
| `app/globals.css` | 전역 스타일(폰트 import, CSS 변수) |
| `app/layout.tsx` | Next.js 루트 레이아웃, 메타데이터 |
| `app/api/calendar/route.ts` | Upstash Redis에 캘린더 데이터 GET/POST하는 API |
| `app/api/parse/route.ts` | **더 이상 프론트에서 호출하지 않음** (아래 4번 참고) |
| `lib/types.ts` | Member/EventItem/RecurringRule/Notice 타입 정의 + 고정 구성원 목록(`DEFAULT_MEMBERS`) |
| `lib/holidays.ts` | 2025~2027년 대한민국 공휴일 데이터 |
| `lib/calendarUtils.ts` | 날짜 계산, 주 단위 그리드 생성, 이벤트 필터링 등 순수 유틸 함수 |
| `lib/redis.ts` | Upstash Redis 클라이언트 초기화 |

## 4. 아직 안 된 기능 / 알려진 이슈

- **`app/api/parse/route.ts`는 죽은 코드**임. 원래 "빠른 일정 입력"과 "공지사항"에 자연어 문장을 넣으면 Gemini API로 날짜를 자동 인식하는 기능이 있었는데, 응답이 30초 이상 걸리거나 끝나지 않는 심각한 성능 문제 때문에 **완전히 제거하고 담당자/날짜/요일을 직접 선택하는 수동 UI로 교체**했다. 이 API 라우트 파일은 지워도 되고 안 지워도 무해함(프론트에서 아무도 안 부름). `GEMINI_API_KEY` 환경변수도 이제 안 쓰지만 지울 필요는 없음.
- **공휴일 데이터가 2025~2027년까지만** 있음. 2028년부터는 달력에 요일별 주말 색만 표시되고 명절 이름은 안 뜸 — 연도가 다가오면 `lib/holidays.ts`에 추가 필요 (설날·추석은 음력이라 매년 실제 날짜를 검색해서 확인해야 함).
- **추석 연휴처럼 공휴일이 있는 주에서, 여러 날짜에 걸친 일정(이어진 막대)과 공휴일 이름 글자가 겹쳐 보이는 버그**가 있었고, `overlayTop` 값 조정(`app/page.tsx`)과 셀 최소 높이 확대(`calendar.css`)로 한 차례 수정했음. **배포 후 실제로 겹침이 해결됐는지 재확인이 안 된 상태** — Claude Code에서 다음에 열면 이 부분부터 눈으로 확인해볼 것.
- 저장소에 `.DS_Store` 파일이 섞여 들어가 있음(맥 자동 생성 파일). 기능엔 무해하지만 정리해도 됨.
- 반복 일정(매주 X요일)을 수정하는 UI는 없고 삭제 후 재등록만 가능함 (편집 기능 없음).
- 여러 날짜 일정(이어진 막대)도 한 번 등록하면 각 날짜별 이벤트로 쪼개져 저장되는데, 수정/삭제 시 그룹 전체가 아니라 날짜별로 따로 처리되는지 재점검 필요.

## 5. 사용자가 중요하게 요청했던 조건

- **가족 구성원은 고정** — 추가/삭제 UI를 만들지 말 것.
- **여러 날짜에 걸친 일정은 반드시 하나로 이어진 막대로 표시** (날짜 칸마다 따로 표시하는 방식 거부).
- **AI(Gemini) 자동 날짜 인식보다 즉시 반응하는 수동 선택 UI를 우선** — 느리면 미련 없이 뺄 것.
- **모바일 전용**으로 최적화할 것 (화면 폭 고정, 터치하기 쉬운 버튼, 입력창 확대 방지).
- **전체적인 색은 화이트/블랙 톤**을 기본으로 하되, 요일(일요일 빨강/토요일 파랑)·공휴일 표시는 색을 유지.
- **타이틀 폰트는 프리텐다드 600 굵기**, 전체 폰트도 프리텐다드로 통일.
- **코드 전달은 항상 "경로 + 전체 교체본"** 형태로 — 부분 diff/패치가 아니라 파일 전체 내용을 다운로드 파일로 받아서, 본인이 직접 GitHub에서 파일 열어 전체 내용을 교체하고 커밋함. Vercel이 자동 재배포.
- **기존에 정상 작동하던 기능은 건드리지 말고 요청 범위만 최소로 수정** (오늘도 호 English 프로젝트에서도 동일하게 요청했던 원칙).

## 6. 다음에 해야 할 작업

1. 배포된 사이트에서 추석 연휴 등 공휴일 주간의 막대/글자 겹침 버그가 실제로 해결됐는지 확인.
2. `app/api/parse/route.ts`(죽은 코드) 삭제 여부 결정하고 정리.
3. 2028년 이후 공휴일 데이터를 언제 추가할지 확인(연말 즈음 필요).
4. 반복 일정 수정 기능(현재는 삭제 후 재등록만 가능) 추가할지 논의.
5. 여러 날짜 일정의 수정/삭제 동작(그룹 단위 vs 날짜별)을 점검하고 필요하면 그룹 단위로 통일.
6. `.DS_Store` 등 불필요한 파일 정리.

## 7. Claude Code가 프로젝트를 열었을 때 먼저 확인해야 할 파일

1. **`lib/types.ts`** — 데이터 구조(Member/EventItem/RecurringRule/Notice)와 고정 구성원 목록을 가장 먼저 파악.
2. **`app/page.tsx`** — 전체 UI/상태/로직이 다 여기 있는 핵심 파일. 매우 길기 때문에 섹션별(달력 그리드 렌더링, 일정 모달, 공지사항, 빠른입력)로 나눠서 읽는 게 좋음.
3. **`app/api/calendar/route.ts`** — 데이터가 어떻게 저장/조회되는지(Upstash Redis, 단일 키 방식).
4. **`lib/holidays.ts`** — 공휴일 데이터 범위와 형식.
5. **`README.md`** — GitHub 업로드부터 Vercel 배포까지의 전체 절차(환경변수 3개: `GEMINI_API_KEY`, `UPSTASH_REDIS_REST_URL`, `UPSTASH_REDIS_REST_TOKEN` — 단, Gemini 키는 현재 실제로는 안 쓰임).
